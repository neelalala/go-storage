logger:
  log_level: DEBUG

database:
  migrations_dir: file://migrations/metadata

grpc:
  address: ":50051"

storage:
  heartbeat_interval: 10s

garbage_collector:
  interval: 1m
  task_limit: 100
  task_timeout: 5s
