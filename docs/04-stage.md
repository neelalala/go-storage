# 04 - Node discovery & Heartbeat

Решил не делить `register` и `heartbeat`, сделать только `heartbeat`, но если
Metadata впервые видит ID узла, то регистрирует узел как новый.

Так же убрал передачу свободного места на диске из 4 этапа и выделил это в
отдельный 5 этап.
