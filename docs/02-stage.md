# 02 - Metadata Service
Решил добавить больше эндпоинтов S3, а именно:
- `GET /` - получить список бакетов
- `PUT /{bucket}` - создать бакет
- `GET /{bucket}` - получить список объектов бакета
- `HEAD /{bucket}` - получить метаданные бакета
- `HEAD /{bucket}/{key...}` - получить метаданные объекта


Решит сделать API уже сейчас похожим на S3. Пришлось ОЧЕНЬ много переписывать: 
- user service пришлось сделать для какой-никакой авторизации 
- очень много новой метаинфомрации хранить:
  - заголовки которые пользователь передал
  - владельца

Постараюсь больше не хвататься за все сразу, сразу выделять просто новые стадии, 
и так эту уже 2 недели пишу, хотя казалась не такой большой